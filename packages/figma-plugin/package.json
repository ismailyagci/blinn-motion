{
  "name": "@blinn-motion/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Blinn Motion Figma plugin — exports a selected Figma Motion animation as a MotionDoc and previews it live with @blinn-motion/dom.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.js",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@blinn-motion/dom": "^0.1.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "^5.9.3"
  }
}
