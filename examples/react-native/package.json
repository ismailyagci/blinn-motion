{
  "name": "@blinn-motion/example-react-native",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web"
  },
  "dependencies": {
    "@blinn-motion/core": "^0.1.1",
    "@blinn-motion/react-native": "^0.1.1"
  },
  "comments": {
    "install": "This is an Expo template. To run it, install the Expo runtime deps (kept out of the workspace install to stay lightweight): `cd examples/react-native && npx expo install expo expo-status-bar react react-native`, then `npm start`. Metro is preconfigured (metro.config.js) to resolve the @blinn-motion/* workspace packages."
  }
}
