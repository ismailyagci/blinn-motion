{
  "name": "blinn-motion",
  "version": "0.1.0",
  "private": true,
  "description": "Blinn Motion — Figma Motion → own document format (MotionDoc) → pure-JS render engine with DOM / Canvas / React / React Native adapters.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @blinn-motion/core && npm run build --workspace @blinn-motion/dom --workspace @blinn-motion/canvas --workspace @blinn-motion/react --workspace @blinn-motion/react-native",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "clean": "rimraf 'packages/*/dist' 'examples/*/dist'",
    "prepare": "husky"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.10.0",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^2.1.9",
    "husky": "^9.1.7",
    "jsdom": "^25.0.1",
    "react-dom": "^18.3.1",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.5",
    "typescript": "^5.9.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
